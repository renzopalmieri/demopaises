import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ButtonTestComponent } from './button-test.component';

describe('ButtonTestComponent', () => {
  let component: ButtonTestComponent;
  let fixture: ComponentFixture<ButtonTestComponent>;
  let parametro = false;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ButtonTestComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ButtonTestComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet1(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet2(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet3(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet4(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet5(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet6(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet7(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet8(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet9(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet10(parametro)).toBeTruthy();
  });  



  it('should create', () => {
    expect(component.testVarGet11(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet12(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet13(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet14(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet15(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet16(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet17(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet18(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet19(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet20(parametro)).toBeTruthy();
  });  



  it('should create', () => {
    expect(component.testVarGet21(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet22(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet23(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet24(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet25(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet26(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet27(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet28(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet29(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet30(parametro)).toBeTruthy();
  });  

 


  it('should create', () => {
    expect(component.testVarGet31(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet32(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet33(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet34(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet35(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet36(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet37(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet38(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet39(parametro)).toBeTruthy();
  });

  it('should create', () => {
    expect(component.testVarGet40(parametro)).toBeTruthy();
  });  
});
